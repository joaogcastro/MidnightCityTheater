export class Sala {
    idSala: number = 0;
    capacidade: string = '';
    tipoSala: string = '';
    preco: number = 0;
}

/*export class Sala {
    idSala: number = 0;
    capacidade: string | null = null; // Alterado para string | null para refletir o tipo C#.
    tipoSala: string | null = null;
    funcionario: Funcionario | null = null; // Adicionei o tipo Funcionario | null.
    idFuncionario: number | null = null;
    filmeId: number | null = null;
    filme: Filme | null = null; // Adicionei o tipo Filme | null.
  }
  
  export class Funcionario {
    // Defina a classe Funcionario conforme necessário.
  }
  
  export class Filme {
    // Defina a classe Filme conforme necessário.
  }*/